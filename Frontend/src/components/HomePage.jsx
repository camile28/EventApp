import React from "react";
import {
  HomePageWrapper,
  HomePageContent,
  HomePageTitle,
} from "../styles/StyledHomePage";

const HomePage = () => {
  return (
    <HomePageWrapper>
      <HomePageContent>
        <HomePageTitle>Welcome to the event registration app</HomePageTitle>
      </HomePageContent>
    </HomePageWrapper>
  );
};

export default HomePage;
